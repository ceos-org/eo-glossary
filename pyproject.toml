[project]
name = "eo-glossary"
version = "2.0.0"
requires-python = ">=3.11"
# Individual scripts declare their own dependencies via PEP 723 inline metadata.
# Use: uv run scripts/export_glossary.py
#      uv run scripts/generate_sigma_graph_data.py
dependencies = []
